<UserControl x:Class="Wpf_EntryPoint.Views.LandingPage_UserController"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Wpf_EntryPoint.Views"
             xmlns:local1="clr-namespace:Wpf_EntryPoint.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <!--<UserControl.DataContext>
        <local1:MainViewModel/>
    </UserControl.DataContext>-->

    <Grid>
        <Grid Margin="10,10,644,348">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBlock HorizontalAlignment="Center" Margin="0,10,0,0" TextWrapping="Wrap" Text="Sms massivi" VerticalAlignment="Top" Width="126"/>
            <StackPanel Margin="10,26,10,348">
                <Button Content="Nuova Lista" Command="{Binding ShowNuovaListaCommand}" />
                <Button Content="NRI" Command="{Binding ShowNRICommand}" />
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
