<Window x:Class="Wpf_EntryPoint.Views.SmsMassivi_RniReduct_UserController"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SmsMassivi_RniReduct_UserController" Height="450" Width="800">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <!-- Input field for the file path -->
        <TextBox x:Name="filePathTextBox" Grid.Row="0" Grid.Column="0" 
                 VerticalAlignment="Center" Margin="0,0,10,0" 
                 Text="{Binding FilePath, UpdateSourceTrigger=PropertyChanged}" />

        <!-- Button to browse the file -->
        <Button Content="Sfoglia" Grid.Row="0" Grid.Column="1" 
                Click="BrowseButton_Click" Width="80" />

        <!-- Button to process the file -->
        <Button Content="Elabora" Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2"
                Click="ProcessButton_Click" Width="100" HorizontalAlignment="Center" 
                Margin="0,20,0,0" />
    </Grid>
</Window>
